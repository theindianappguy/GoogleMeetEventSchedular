name: google_meet_app
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^0.5.1
  cloud_firestore: ^0.14.2
  intl: ^0.16.1
  googleapis: ^0.56.1
  googleapis_auth: ^0.2.12
  url_launcher: ^5.7.8
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
